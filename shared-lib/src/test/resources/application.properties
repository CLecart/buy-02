# Test resources should not contain deterministic secrets committed to VCS.
# Tests must generate a test-only JWT secret at runtime using DynamicPropertySource or a test utility.
# APP_JWT_SECRET must NOT be committed here.
