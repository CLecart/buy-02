# Copy to .env and fill secrets (do NOT commit .env)
APP_JWT_SECRET=change-me-replace-with-strong-secret
MONGO_URI=mongodb://root:example@mongo:27017/buy02?authSource=admin
STORAGE_PATH=./data/storage

# SSL/TLS Configuration (set to true for production)
SSL_ENABLED=false
SSL_KEYSTORE_PASSWORD=changeit
