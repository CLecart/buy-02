# GitHub Configuration

- All development, PRs, reviews, merges, and CI/CD are managed on GitHub.
- Gitea is used only as a mirror (push only, no CI/CD, no PR).
- Branches are synchronized automatically.
- No manual actions on Gitea except verification.

See README.md for process evidence.
